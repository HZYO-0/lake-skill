version: '3.8'

services:
  lake-skill:
    build: .
    volumes:
      - ./input:/app/input
      - ./work:/app/work
      - ./kb:/app/kb
      - ./reports:/app/reports
      - ./examples:/app/examples
    command: ["--help"]
